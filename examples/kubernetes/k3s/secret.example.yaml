apiVersion: v1
kind: Secret
metadata:
  name: terraform-registry-bootstrap
  namespace: terraform-registry
type: Opaque
stringData:
  # Replace before applying. Used only to bootstrap a missing keys.json.
  api-key: REPLACE_WITH_A_LONG_RANDOM_SECRET