# Terraform Registry - Quick Start Docker Compose
#
# Usage:
#   docker-compose up -d              # Start registry
#   docker-compose logs -f            # View logs
#   docker-compose down               # Stop registry
#
# Registry will be available at: http://localhost:5000
# Web UI at: http://localhost:5000/ui
#
# Optional: Set REGISTRY_API_KEY to require auth for management operations
#
# For more examples, see:
#   - examples/docker-compose/        Full examples (filesystem + S3)
#   - examples/kubernetes/            Kubernetes deployment
#   - examples/s3/                    Production S3 setup

version: '3.8'

services:
  terraform-registry:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/brandencobb/terraform-registry:latest
    container_name: terraform-registry
    ports:
      - "5000:8080"
    volumes:
      - ./data:/var/lib/terraform-registry
    environment:
      STORAGE_TYPE: filesystem
      BASE_URL: http://localhost:5000
      PORT: 8080
      # Uncomment and set to require API key for management operations:
      # REGISTRY_API_KEY: "your-secret-key"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
